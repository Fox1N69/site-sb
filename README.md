# 🌟 Site-SB

Современное веб-приложение для управления контентом, построенное с использованием Go (Gin), Astro, React/Svelte и PostgreSQL.

## 🏗️ Архитектура

- **Backend**: Go + Gin Framework + GORM
- **Frontend**: Astro + React/Svelte + TailwindCSS + NextUI
- **База данных**: PostgreSQL
- **Контейнеризация**: Docker + Docker Compose
- **Веб-сервер**: Nginx (reverse proxy)
- **SSL**: Let's Encrypt (Certbot)

## ⚡ Быстрый старт

### 🚀 Автоматическая установка на Ubuntu Server

```bash
# Скачать и запустить скрипт быстрой установки
curl -fsSL https://raw.githubusercontent.com/your-username/site-sb/main/scripts/quick-install.sh | bash
```

### 📋 Требования для автоматической установки

- Ubuntu Server 20.04+ (рекомендуется 22.04 LTS)
- Минимум 2 GB RAM
- Минимум 20 GB свободного места
- Доменное имя, указывающее на ваш сервер
- SSH доступ с правами sudo

## 🔧 Ручная установка

### Локальная разработка

1. **Клонирование репозитория**
   ```bash
   git clone https://github.com/your-username/site-sb.git
   cd site-sb
   ```

2. **Запуск с Docker Compose**
   ```bash
   docker-compose up --build
   ```

3. **Доступ к приложению**
   - Frontend: http://localhost:4321
   - Backend API: http://localhost:4000
   - PostgreSQL: localhost:5432

### Production развертывание

Подробная инструкция находится в [DEPLOYMENT.md](DEPLOYMENT.md)

## 📖 Документация

- **[Полная инструкция по развертыванию](DEPLOYMENT.md)** - Пошаговое руководство для Ubuntu Server
- **[Архитектура проекта](CLAUDE.md)** - Техническая документация

## 🛠️ Команды разработки

### Frontend (Astro + React/Svelte)
```bash
cd frontend
yarn dev        # Запуск dev сервера (порт 4321)
yarn build      # Сборка для продакшена
yarn preview    # Предварительный просмотр сборки
```

### Backend (Go + Gin)
```bash
cd backend
go run cmd/app/main.go          # Запуск dev сервера (порт 4000)
go build -o bin/app cmd/app/main.go  # Сборка бинарного файла
go test -short ./...            # Запуск тестов
go mod tidy                     # Очистка зависимостей
```

### Docker
```bash
# Разработка
docker-compose up --build

# Production
docker-compose -f docker-compose.prod.yaml up --build -d
```

## 🔌 API Endpoints

### Публичные
- `GET /post/` - Получить все посты
- `GET /post/:id` - Получить пост по ID

### Аутентификация
- `POST /v1/account/auth/register` - Регистрация пользователя
- `POST /v1/account/auth/login` - Вход пользователя

### Админ панель (требует аутентификацию)
- `POST /admin/post/create` - Создать пост
- `PATCH /admin/post/update/:id` - Обновить пост
- `DELETE /admin/post/delete/:id` - Удалить пост

## 🐳 Docker Конфигурация

### Сервисы
- **postgres**: PostgreSQL 15 Alpine с persistent storage
- **backend**: Go приложение с автоматическими миграциями
- **frontend**: Astro SSR приложение
- **nginx**: Reverse proxy с SSL (только в production)

### Сети
- **app-network**: Bridge сеть для связи между сервисами

### Volumes
- **postgres_data**: Persistent storage для PostgreSQL
- **./backups**: Директория для бэкапов базы данных

## 🔒 Безопасность

- JWT токены для аутентификации
- HTTPS с автоматическими SSL сертификатами
- CORS middleware
- Роль-based доступ к админ панели
- Автоматические бэкапы базы данных
- Firewall конфигурация

## 🚀 Скрипты автоматизации

### Основные скрипты
- `scripts/quick-install.sh` - Полная автоматическая установка
- `scripts/deploy.sh` - Полное развертывание на сервере
- `scripts/update.sh` - Обновление приложения с rollback

### Системные утилиты (создаются при установке)
- `site-sb-status` - Проверка статуса приложения
- `site-sb-logs` - Просмотр логов
- `site-sb-restart` - Перезапуск сервисов
- `site-sb-backup` - Создание бэкапа базы данных
- `site-sb-start/stop` - Запуск/остановка приложения

## 📊 Мониторинг

### Автоматические задачи
- Ежедневные бэкапы в 2:00 (30 дней хранения)
- Еженедельные отчеты о статусе
- Автоматическое обновление SSL сертификатов
- Ротация логов

### Проверка здоровья
```bash
# Статус всех сервисов
site-sb-status

# Логи в реальном времени
site-sb-logs

# Использование ресурсов
docker stats
```

## 🔄 CI/CD

GitHub Actions workflow для автоматического развертывания:
- Автоматический deploy при push в main ветку
- Проверка работоспособности после развертывания
- Rollback в случае ошибок

## 🗂️ Структура проекта

```
site-sb/
├── backend/                 # Go backend
│   ├── cmd/app/            # Точка входа приложения
│   ├── internal/           # Внутренняя логика
│   │   ├── api/           # HTTP handlers
│   │   ├── service/       # Бизнес логика
│   │   ├── repo/          # Слой данных
│   │   └── models/        # Модели данных
│   ├── common/            # Общие утилиты
│   ├── config/            # Конфигурация
│   └── infra/             # Инфраструктура (DB, config)
├── frontend/               # Astro frontend
│   ├── src/
│   │   ├── pages/         # Маршруты
│   │   ├── components/    # Компоненты
│   │   ├── layouts/       # Макеты
│   │   └── assets/        # Статические ресурсы
│   └── public/            # Публичные файлы
├── scripts/               # Скрипты развертывания
├── .github/workflows/     # CI/CD
├── docker-compose.yaml    # Development
├── docker-compose.prod.yaml # Production
└── DEPLOYMENT.md          # Инструкция по развертыванию
```

## 🤝 Разработка

### Добавление новых функций
1. Создайте ветку от `main`
2. Внесите изменения
3. Протестируйте локально с `docker-compose up --build`
4. Создайте Pull Request

### Правила коммитов
- `feat:` - новая функциональность
- `fix:` - исправление багов
- `docs:` - изменения документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов

## 📞 Поддержка

### Логи и отладка
```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend

# Вход в контейнер для отладки
docker-compose exec backend sh
docker-compose exec postgres psql -U postgres sitesb
```

### Частые проблемы
1. **Порты заняты**: Остановите конфликтующие сервисы
2. **Проблемы с базой данных**: Проверьте логи PostgreSQL
3. **SSL не работает**: Убедитесь, что DNS записи правильные
4. **Frontend не доступен**: Проверьте переменные окружения API_URL

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE) файл для деталей.

## 🌟 Благодарности

- [Gin](https://gin-gonic.com/) - HTTP веб фреймворк для Go
- [Astro](https://astro.build/) - Современный веб фреймворк
- [PostgreSQL](https://www.postgresql.org/) - Надежная база данных
- [Docker](https://www.docker.com/) - Контейнеризация
- [TailwindCSS](https://tailwindcss.com/) - CSS фреймворк

---

Сделано с ❤️ для современной веб-разработки